#include "main.h"
#include <stdlib.h>

/**
 * _abs - returns the absolite value of an integer
 * @n: number to be checked
 *
 * Return: always success
 */
int _abs(int n)
{
	int x = abs(n);

	return (x);
}
