#include "main.h"
#include <string.h>

/**
 * _strcat - joins two strings
 * @dest: destination
 * @src: source
 *
 * Return: joined string
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
