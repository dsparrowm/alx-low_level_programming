#include "main.h"

/**
 * _isupper  - checks for uppercase letter
 * @c: character to be checked
 *
 * Return: 0
 */
 int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
