#include "main.h"
#include <math.h>

/**
 * _pow_recursion - prints the number to the power of another number
 * @x: first number
 * @y: second number
 *
 * Return: the power of x to y
 */
int _pow_recursion(int x, int y)
{
	return (pow(x, y));
}
